name: Deploy to GitHub Pages
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [13.2.0]
    steps:
      - uses: actions/checkout@master
      - name: install, build, and test
        run: |
          npm install
          npm run build
        env:
          CI: true
      - name: Deploy to production
        uses: awb-pw/deploy-to-github-pages-action@1.1.1
        env:
          AUTHOR: Amy Liu
          BUILD_PATH: public
          GITHUB_PAGE_NAME: amyliu840.github.io
          PA_TOKEN: ${{ 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 }}
          USERNAME: amyliu840
          EMAIL: liuamy840@hotmail.com